1. Make sure `python-pipx` package is installed.
2. `pipx ensurepath`: *Does exactly one thing: ensure that python, pip and with pip installed binarys are in your path.*: https://pypi.org/project/ensurepath/
3. `pipx install konsave`: to be able to export KDE customizations: https://github.com/Prayag2/konsave - doesn't seem to work reliably.